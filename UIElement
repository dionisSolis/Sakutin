using System;

namespace qwerty
{
    class Program
    {
        static void Main(string[] args)
        {
            DrawBar(75, 0, 0, 100, 5);
        }

        static void DrawBar(int value, int coordinateX, int coordinateY, int maxValue, int step)
        {
            char openBracket = '[';
            char closeBracket = ']';
            char fullPoint = '#';
            char emptyPoint = '_';

            string bar = "";

            bar += openBracket;

            for (int i = 0; i < maxValue; i += step)
            {
                if (i < value)
                {
                    bar += fullPoint;
                }
                else
                {
                    bar += emptyPoint;
                }
            }

            bar += closeBracket;
            Console.SetCursorPosition(coordinateX, coordinateY);
            Console.WriteLine(bar);
        }
    }
}
