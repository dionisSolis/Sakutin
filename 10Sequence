using System;

namespace qwerty
{
    class Program
    {
        static void Main(string[] args)
        {
            int firstDigit = 5;
            int lastDigit = 96;
            int stepToPlusDigit = 7;

            for (int i = firstDigit; i <= lastDigit; i += stepToPlusDigit)
            {
                Console.Write(i + " ");
            }
        }
    }
}
